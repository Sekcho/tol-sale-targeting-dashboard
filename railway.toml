[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app_sales_v2:server --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
